# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build artifacts
dist/
dist-ssr/
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Node
node_modules/

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock

# Bundled binaries (downloaded by scripts/fetch-ffmpeg.mjs, not committed —
# each build host fetches its own copy).
src-tauri/binaries/*.exe
src-tauri/binaries/ffmpeg-*
src-tauri/binaries/ffprobe-*

# Local secrets / API credentials
.env
.env.local
*.local.json

# Personal project notes (private; not for the repo)
NOTES.md

# Local project documentation (private; not for the repo)
docs/

# Generated TypeScript build info
*.tsbuildinfo
